{
  "name": "incode-web-app",
  "version": "1.0.0",
  "description": "\"A web app designed to aid the INCODE students at INCO Academy with consolidating information.\"",
  "main": "",
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.1"
  }
}
