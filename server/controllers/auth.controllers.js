exports.registerUser = (req, res) => {
  // logic for registering a new user. Include validation in routes file as middleware
}

exports.loginUser = (req, res) => {
  // logic for authorising a user. Include validation in routes file as middleware
}
